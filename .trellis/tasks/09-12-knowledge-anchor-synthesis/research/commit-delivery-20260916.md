# 分功能提交与中文注释核验

2026-09-16。用户授权按功能分批提交并推送，明确提交说明和新增手写代码注释使用中文。用户另行明确：独立全项目规范整理暂不纳入；已执行迁移和自动生成代码的英文注释保留原文。

## 提交范围

1. 资料发现：目录扫描、来源存在性和版本、画像知识目录、失败恢复基础。
2. 模型配置：默认/分功能思考强度、双进程模型构造与探测、BGE兼容、模型设置页面。
3. 主笔记后端：锚点范围与来源关联、目标初版、增量融合、正文依赖传播、人工全文复核与冲突、补源恢复、历史发布；同时提交共享组装入口、Atlas100–135及其校验清单/Schema、OpenAPI和生成客户端。
4. 主笔记工作台：集中阅读、来源追溯/图谱、关联与范围审核、局部更新、人工全文审阅、重合并、历史恢复和路由。
5. 需求与验收证据：PRD、ADR、领域词汇、实施和实际核验记录。功能提交之后按Trellis流程单独归档当前任务并记录会话。

以上为审阅分组。资料/模型基础依赖同批次后续的共享迁移和运行时接线，不能把中间提交声称为独立可部署版本；完整交付以最终推送的dev分支为准。既有业务验证、真实模型合成验收与本地服务恢复证据沿用remaining-acceptance.md，不重复付费调用。

## 中文注释与代码等价

- 三个互不重叠范围完成翻译：Organizing适配层95文件/316组，Organizing核心82文件/200组，其他模块63文件（55个Go、7个TS/TSX、1个环境示例，含末轮两组补译）。仅处理本次新增或修改的说明性注释。
- 主会话用翻译前工作树归档逐文件核验375个Go文件：实际232个文件发生注释变化，所有非注释token、字面量与go:build等机器指令保持一致。新增英文自然语言Go注释扫描为0；未改动的旧英文注释保留。
- 主会话检查72个TS/TSX/MJS/CSS/环境示例文件：其中8个注释变化文件在TypeScript去注释编译输出或环境有效行上保持一致。7个实际前端修改文件的定向ESLint通过。
- 全部受影响Go文件gofmt检查和原已跟踪差异检查通过。提交前扩展到全部暂存文件时，发现00103/00107/00110/00121/00125五个已执行迁移原有末尾空行；严格保留迁移字节及校验和，定点记录这五项格式例外。排除该类末尾空行后的暂存空白检查通过，未放过其他诊断。OpenAPI契约检查及258个operation tags校验通过。
- 翻译前后118个Atlas、生成客户端、OpenAPI及排除规范文件逐字节一致；SQL迁移100–135与校验和未改，模型提示词及所有运行字符串保持原字节。

## 保留范围与发布边界

- 71个独立 .trellis/spec 改动按用户要求保留在工作区，不暂存；主会话保存并核对它们的内容哈希，未改写。
- 三份并行研究临时文件（00124 SQL副本、旧OpenAPI片段、manuscript-boundary-check/main.go）保留在本地任务目录，不混入生产代码提交。正式版本均在Atlas/OpenAPI/应用源码中。
- 候选路径未检出真实API Key、私钥或访问Token；仅live诊断测试含明确的假密钥canary。私有 .env、.zhixu、日志、配置快照和笔记内容不在提交范围。
- Git提交与push不会补充完整登录AppShell和整套真实个人笔记的使用验收；该限制保持。

提交哈希及远端同步结果记录到本次会话日志；所有提交采用中文标题和说明，不强推、不改写既有历史。
